"""
RoadVision — Module: YOLO Detector (M2)
=======================================
Responsibility:
    Wrap the YOLO model to produce per-frame detections.

Inputs:
    - Frame (numpy array, BGR)

Outputs:
    - List of Detection objects:
        { class_name, confidence, bbox (x1,y1,x2,y2), centroid (cx,cy) }

NOTE: This file is a STUB. Implementation begins in Phase 1.
Training strategy (pretrained init vs. from scratch) must be confirmed first.
"""

# detector.py — Placeholder
